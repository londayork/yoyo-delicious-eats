import React from "react";
import ReactDOM from "react-dom/client";
import YoYosStore from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <YoYosStore />
  </React.StrictMode>
);
